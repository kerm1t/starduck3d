there is a number of generators:
FlatArc,Clothoid,Snake(only used for trajectory),Straight,Zebra

other classes:
- SixDofGenerator
- CreateTrajectory

idee: split up in MVC, etwa: V=GUI stuff, M=all generators

the export function contains generators for
- trajectory, what is the difference to CreateTrajectory ?
- road



1. vectorwise (as done currently) <- Python
2. blockwise () <- C++ OpenGL
3. buggyboy